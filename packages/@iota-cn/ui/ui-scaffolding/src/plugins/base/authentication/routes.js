import Signin from './view/Signin'

const routes = {
    iota: {
        app: {
            signin: {
                path: 'signin',
                component: Signin,
            }
        }
    }
}

export default routes